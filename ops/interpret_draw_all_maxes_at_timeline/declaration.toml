type = "program"
description = "draw all maxes at graphic with timeline"

[properties]
module = "file://localhost/$PWD/interpret_draw_all_maxes_at_timeline"
entrypoint = ["python", "main.py"]

[properties.managers]
environment = { provider = "miniconda", name = "microseismic" }

[[properties.managers.package]]
provider = "pip"
dependencies = "file://localhost/$PWD/requirements.txt"

[[properties.languages]]
name = "python"
version = "3.10"
