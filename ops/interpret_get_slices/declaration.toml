type = "program"
description = "read traces"

[properties]
module = "file://localhost/$PWD/interpret_get_slices"
entrypoint = ["python", "main.py"]

[properties.managers]
environment = { provider = "miniconda", name = "microseismic" }

[[properties.languages]]
name = "python"
version = "3.10"
