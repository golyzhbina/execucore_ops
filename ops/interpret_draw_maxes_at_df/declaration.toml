type = "program"
description = "draw maxes at detect func"

[properties]
module = "file://localhost/$PWD/interpret_draw_maxes_at_df"
entrypoint = ["python", "main.py"]

[properties.managers]
environment = { provider = "miniconda", name = "microseismic" }

[[properties.languages]]
name = "python"
version = "3.10"
